services:
  kanji:
    build: .
    ports:
      - "52654:52654"
    volumes:
      - kanji-data:/data
    environment:
      - KANJI_ADMINS=${KANJI_ADMINS:-}
    restart: unless-stopped

volumes:
  kanji-data:
